platform: linux

image:
  type: registry-image
  source: { repository: gradle, tag: 7.3.0-jdk17 }

# Cache the Gradle repository directory
caches:
  - path: $HOME/.gradle

inputs:
  - name: code

run:
  path: bash
  args:
    - -exc
    - |
      cd code
      make test
